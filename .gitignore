target/
# Cargo.lock is committed — workspace ships binaries and CI uses
# --locked. (Removed from gitignore 2026-05-17 because every CI
# job failed with 'cannot create the lock file because --locked
# was passed'.)
*.swp
*~
.DS_Store

# Local content-store fixtures (the canonical store lives elsewhere)
cms-store/
/tmp/

# Python bytecode
__pycache__/
*.pyc
